#include<stdio.h>
#include<limits.h>
int main(){
    int arr[5];
    for (int  i = 0; i <=5-1; i++)
    { int max = arr[i];
        printf("enter the  elemnts %d : ",i);
        scanf("%d",&arr[i]);if (max<arr[i]);
    }
    return 0;}
                       
       // code not copleated
                       