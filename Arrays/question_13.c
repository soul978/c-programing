//. what is diffrent between the 5's in these two expression?

#include<stdio.h>
int main(){
    int num[5];// its create a  five contener 
    num[5] = 11;//  insert the elemnets in the contenters



    // ans = first is array size , second is particular elements
    return 0;
}