#include<stdio.h>
#include<math.h>
int main(){
    // float a; //! calculate the power of the given values;
    // a = pow(3.0,2.0);
    // printf("%f",a);

    int i;
    float b;
    i = 3.0;
    b=4;
    printf("%d %f",i,b);

    return 0;
}